package advance

import java.util.*

fun main(args: Array<String>) {
    val oldDate = Date()
    val newData = Date()

    val ans = oldDate + newData
}

operator fun Date.plus(time: Date) {
    //date
}